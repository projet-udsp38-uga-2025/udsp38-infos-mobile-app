import 'package:flutter/material.dart';

class ActualitesScreen extends StatelessWidget {
  const ActualitesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Page d\'actualités')),
    );
  }
}